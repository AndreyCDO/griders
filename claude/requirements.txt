# Cryptorg MCP Server — зависимости
# Установка: pip install -r requirements.txt

# MCP SDK (Anthropic)
mcp>=1.0.0,<2.0.0

# HTTP клиент (async)
httpx>=0.27.0,<1.0.0

# Переменные окружения
python-dotenv>=1.0.0,<2.0.0

# Python 3.11+ требуется
